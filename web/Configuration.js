//Configuration.js

class Configuration {
	constructor(windowInstance, ethereumContract){
     this.window = windowInstance;
     this.contract = ethereumContract;
    }
}

module.exports = Configuration;